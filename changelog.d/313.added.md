`run_cnaster_port` decoded integer copies under the cap `int_copy_num.max_total_copy` states, for the total and each allele, where `cnaster` fixed them at 6 and 5; `--no-copy-cap` restored `cnaster`'s caps.
