`spatial_figure` named clones by their integer copy profile by default, so clones that decode to the same `(A, B)` at every bin are one clone (`labels="continuous"` for the fit's own), and `run_cnaster_port` wrote `clone_labels_integer.tsv` with both labels per spot.
