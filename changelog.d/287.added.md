`port.extensions.parameter_errors` returns a fit's covariance from the Hessian of the `jax` objective in `port.extensions.jax_hmm`, refuses a point that is not a maximum, and propagates the `log Z_c` debiasing through its Jacobian, so `decode_copy_state` has a covariance to read. `jax` is a new dependency.
