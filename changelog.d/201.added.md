Added `run_cnaster_port`, which runs `cnaster`'s pipeline with every `port` replacement rebound into it, with `--no-patch` for the unpatched arm and `--time-stages` for what the replacements cost in the run.
