`run_cnaster_port --sal` substituted `snakes_and_ladders` routines where `port` measured a gain; it admitted the clone labelling (alpha expansion with the Rust minimum cut, then `cnaster`'s ICM for its clone floor), which recovered the dev instance's planted clones at ARI 1.000 against 0.919.
