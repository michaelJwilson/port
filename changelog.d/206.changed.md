`run_cnaster_port` now replaces `pipeline_clone_assignment`, so the fused spot/clone field runs in a whole pipeline: no `(n_states, n_obs, n_spots)` emission array, and the COO adjacency round trip only when a merge needs it.
