`run_cnaster_port` now writes each clone's per-bin integer copies from `lattice_decode` (per-clone tumour fraction, parsimony 0.5) to `cnv_seglevel.tsv`, `cnv_genelevel.tsv`, the per-state table (each state's most frequent pair) and the genomic figures, and records the fitted fractions in `copy_decode.tsv`; `--copy-decode shared` restores the previous per-state decode. `lattice_decode`'s fraction step no longer moves uphill.
