`run_cnaster_port` wrote `cnv_states.tsv`, `cnv_segments.tsv`, `cnv_binlevel.tsv` and `manifest.json` beside `cnaster`'s files: the map from fitted states to integer copies per clone, the runs of equal `(A, B)`, the posterior-mean `mu` and `p` per bin, and the run's provenance; `--no-outputs` skips them.
