`port.patch.summaries.summarize_blocks` logs `cnaster`'s block breakdown from
two passes and a `bincount` rather than a per-block slice of the count matrix,
taking `port.patch.omics.assign_initial_blocks` from 2.1x to 11.6x.
