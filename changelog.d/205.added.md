Added `port.patch.lattice` and `port.patch.emission`: one forward/backward recursion in place of `cnaster`'s four, and one emission entry point in place of its two, writing into buffers the caller owns instead of allocating `(n_states, n_obs, n_spots)` per channel per call.
