`port.patch.hmrf_field` carries a drop-in replacement for `cnaster.hmrf.compute_loglike_spot_assignment` that walks the spot axis innermost, measured 3.90-7.17x faster at `cnaster`'s own layout and pinned bitwise (#59 item 1).
