`spatial_figure` labelled its rows on (a) alone: (b), which shares them, kept its ticks and dropped its labels, and both panels grew into the width that freed.
`genomic.pdf` is 1.5 in shorter than the text block, leaving room for its caption; `combined.pdf` puts the spatial figure over the genomic one on one page; and the copy-number profile's hatch lines are as wide as the fill between them.
