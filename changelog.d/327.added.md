`run_cnaster_port --copy-likelihood` re-decoded each clone's integer copies by the HMM's pseudobulk likelihood, with the fitted path held and the neutral state at `(1, 1)`, starting from `cnaster`'s MILP answer.
