`README.md` now carries five badges — three coverage guards and the patched-against-unpatched runtime and memory ratios — derived from `.badges/measurements.json` by `python -m tests.badges`, with a test that fails when the two disagree.
