`port.patch.hmrf_fused_field` computes `cnaster`'s spot/clone field in one pass over the counts, materializing no `(n_states, n_obs, n_spots)` emission -- 2.08x where `n_clones < n_states`, and it runs at sizes the two-step is killed at (#59 item 2).
