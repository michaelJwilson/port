`run_cnaster_port --clone-mixture` fits each clone's pseudobulk as a mixture of the clones' integer-copy paths and a diploid normal, and scores spots against each clone's pure path at its own normal fraction.
