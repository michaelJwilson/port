`run_cnaster_port --copy-errors` writes `cnv_copy_sets.tsv`: every integer `(A, B)` inside each fitted state's 95 per cent credible region, from the fit's error bars in the neutral pin's scale (#353).
