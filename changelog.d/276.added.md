`port.patch.hmm_nophasing` can fold `cnaster`'s per-clone library normalizer `log Z_c` into the fitted `log_mu`, which `cnaster` computes and discards. It is **off by default**; `logmu_shift()` enables it for a block, and off, the emission is `cnaster`'s bitwise.
