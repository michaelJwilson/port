`port.patch.icm_interface` presents `cnaster.icm.icm_sweep_deque`'s fifteen parameters as eight -- the dead `posterior` dropped, the per-sample weights and the allowed-clone mask folded into the field, `temp` folded into the coupling, and the graph passed as one object -- pinned bitwise against the fifteen-argument call (#59 item 5).
