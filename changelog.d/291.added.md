`port.extensions.realization_plot` draws fitted `(mubar, p)` per copy state over realizations of one planted genome: one realization with error bars and 1- and 2-sigma contours from `parameter_errors`, the others as points, and the truth.
