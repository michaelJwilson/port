`plot_clones_genomic` took `colour_by`, `"integer"` for deduplicated `(A, B)` colours or `"states"` for one colour per fitted state labelled with its `2mu` and `p`, and `run_cnaster_port --genomic-colours` set it for a run.
