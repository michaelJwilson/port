`run_cnaster_port` no longer hangs in `initialize_rectangular_clones` when a block of the Dirichlet-drawn partition is too small for any clone assignment to pass `cnaster`'s 20 per cent test; the boundaries are redrawn after 1,000 tries, and the result is `cnaster`'s, bitwise, wherever `cnaster` returns.
