`run_cnaster_port` decodes integer copies by the HMM's pseudobulk likelihood only, with the pinned `mu`, each clone's `logmu_shift` (recorded in `new_log_mu_shift`), its decoded path and the fitted dispersions held, and the shared pinned normal state at `(1, 1)`; `--copy-likelihood` is removed.
