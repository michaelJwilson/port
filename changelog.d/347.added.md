Added `run_calicost`, which runs CalicoST on a `run_cnaster` configuration and its fixture files, and three clone-assignment patches on by default in `run_cnaster_port`: `--floor-merge`, `--refinement-mask` and `--distinct-init`.
