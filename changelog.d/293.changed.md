`run_cnaster_port` now fits with the per-clone `logmu_shift = -log sum_g lambda_g mu` folded into every emission the fit, the final decode and the clone assignment read. It then pins the balanced state with the lowest `mu` to `mu = 1`, once, before integer copy and plotting. On by default; `--no-shift` restores `cnaster`'s unshifted model.
