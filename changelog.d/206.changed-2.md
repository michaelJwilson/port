`run_cnaster_port`'s clone assignment now installs all five of #59's seam patches: the field written into a caller's buffer, the COO adjacency built by three array expressions where it is consumed, the loop invariants hoisted out of the outer loop, and a solver taking four arguments where `cnaster` passes fifteen.
