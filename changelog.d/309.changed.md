`run_cnaster_port` drew the copy-number profile one row per clone, with each aberrant segment filled in A's colour and hatched in B's -- at +45 degrees where A >= B and -45 where A < B, so mirrored segments hatched opposite ways -- in place of cnaster's two half-rows and chevrons; `--no-figures` restored cnaster's.
