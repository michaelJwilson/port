`combined_figure`'s one page became two figures at `llncs`'s `\textwidth`, 122 mm (4.80 in), each written at exactly its size, at 7 pt throughout with one 0.5 pt line weight. `genomic_figure` fills the 193 mm text block: (a) the tracks, points coloured by HMM state and keyed by decoded copy pair with one dot per state, over (b) the profile under its "Mirror" and $\mathbb{N}$-CNA key, on one left and one right edge, the white between clones halved, ticks at both ends and the middle to one decimal, names and labels in one column 6 pt in. `spatial_figure` is about a quarter of the block tall: (a) the H&E slide and (b) the clones, square and as large as fit across, framed, ticked at their spots' first, middle and last coordinate, (b) keyed on the right edge. Clones are named $m_N$, $m_1$, .... The profile fills and hatches in the colours of the $\mathbb{N}$-CNA bar's boxes, whose scale drops `khaki` for one more red, each aberration bounded left and right. `generate_plots` writes `genomic.pdf` and `spatial.pdf` in place of `combined.pdf`.
