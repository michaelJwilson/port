`combined_figure` drew its page at `llncs`'s `\textwidth`, 122 mm (4.80 in), and `tests.generate_plots` wrote it at exactly that size; (b)'s axis spanned (a)'s tracks, its clone names left-aligned, its rows taller and its hatch drawn as lines at 25 degrees 0.035 in apart with swatches `h=0` and `h=1`; the H&E slide moved to (c), left, and (d)'s clones were keyed beside it in two columns.
