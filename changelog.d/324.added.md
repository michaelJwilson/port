`run_cnaster_port` audited its configuration against what `cnaster` reads -- keys nothing read, `em_*` tolerances the solver did not take, numbers loaded as strings, thresholds no state could cross, a clone floor that did not govern, and keys only `port` reads (`int_copy_num.max_total_copy`, applied by `--copy-cap`) -- printed the count on every run, and listed the findings under `--audit-config`.
