`port.oxiport` gained two CSR reduction kernels, `csr_pair_row_sums` and
`csr_positive_per_column`, and `port.patch.input_data.load_input_data` a
`backend` keyword choosing between them and the `numpy` forms.
