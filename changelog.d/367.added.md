`port.extensions.copy_likelihood.lattice_decode` decodes each clone's integer copies with one HMM state per `(A, B)`, a per-clone tumour fraction and a parsimony prior of 0.5 nats per bin; the pipeline's per-state decode is `shared_decode`, and the decoders compared and set aside are in `port.sandbox.integer_decoding`.
