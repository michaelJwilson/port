`plot_clones_spatial` took `sample_layout=(rows, columns)` to draw one panel per sample in its own coordinates, `run_cnaster_port --sample-layout` set it for a run, and `port.extensions.multisample` added the cross-sample adjacency placeholder.
