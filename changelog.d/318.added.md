`run_cnaster_port` ran `cnaster`'s forward and backward lattices from `port`'s Rust backend by default, bitwise `cnaster`'s. This removed the per-process `numba` compile of the unphased pair and cut a dev-instance run from 36.5 s to 29.8 s; `--no-rust` restored `cnaster`'s kernels.
