`run_cnaster_port`'s genomic figures draw each clone's RDR line at `exp(log_mu - log Z_c)`, the level that clone's points read at, when the shift is on. The pinned state is now the normal clone's dominant balanced state, not the balanced state with the lowest `mu`; on the dev instance the old rule pinned a 57-bin sub-neutral state and drew every line 2.40 times high.
