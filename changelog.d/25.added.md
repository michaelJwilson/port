`port.integer_copy` decodes a fitted copy state into the *set* of integer allele copies consistent with its credible region, as `integer_copy_numbers.tex` specifies, rather than the single argmin `cnaster` returns.
