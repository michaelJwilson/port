`python -m port.sim.run_sim_gen <manifest>.toml` draws a simulated sample in CalicoST's `sim/<name>/` format from a TOML manifest (`port.sim.toml_manifest`) stating its sizes, clones, per-clone `normal_frac`, dispersions and fitted coverage laws; `normal_frac = 0` draws pure tumour spots.
