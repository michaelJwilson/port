`port.patch.hmrf_invariants` computes the boundary's per-spot valid-segment counts and the relative channel weight derived from them once, rather than on every outer iteration -- 25.2 ms per iteration at 3,000 by 5,000, for quantities `cnaster` conditions on and never fits (#59 item 4).
