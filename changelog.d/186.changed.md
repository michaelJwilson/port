`port.patch.io.load_input_data`'s `sparse_counts=True` now also returns
the count layer and `exp_counts` sparse, and both return paths read the two
allele files on two threads: 2.18x and 1.43x less peak against `cnaster` at
2,500 spots.
