"""The planted instances `cnamaste`'s tests run on, and the files they go out as.

`truth` plants and draws; `unsegment` takes bins back to genes and blocks;
`inputs` writes the files `run_cnamaste` reads; `config` writes its
configuration; `outputs` reads what a run wrote back against the truth.
"""
