"""Work `port` has set aside: measured, off by default, outside the claims.

`tests/test_module_correspondence.py` gives every module one of four jobs and
sends what has none here. Nothing in this package is installed by
`run_cnaster_port`, no `SWAPS` row names it, and `pyproject.toml` omits it
from the coverage denominator: a module here is an experiment with its
numbers attached, not a replacement anyone runs by default.
"""
