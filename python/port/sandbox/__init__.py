"""Code set aside: outside the coverage denominator and outside the claims.

`tests/test_module_correspondence.py` sends a module with none of the four
jobs here, and `pyproject.toml` omits it from coverage.
"""
