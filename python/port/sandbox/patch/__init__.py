"""Patches whose `cnaster` subject the current pin no longer carries.

`loh_density` replaced `cnaster.plot_loh_density.plot_loh_density`, which
`cnaster@port#23cae59` deletes and `run_cnaster` no longer calls. Parked
rather than deleted until the subject returns or the work is dropped.
"""
