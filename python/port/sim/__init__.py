"""Simulation: the work that needs planted truth.

One of the four jobs `CLAUDE.md` gives a module. Nothing here replaces a
`cnaster` name and nothing here is reached by a swap row; these generate the
instances the referees judge against, which is why they are neither a patch
nor an extension.

`run_sim_gen` was written for `cnaster` and proposed there (#116); it lives
here until that lands, and moves out rather than being duplicated if it does.
"""
